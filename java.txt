1. Write a java program to read ‘n’ names of your friends, store it into linked list, also display
contents of the same.

import java.util.*;
import java.io.*;

class Friends {
    public static void main(String[] args) throws IOException {
        LinkedList al = new LinkedList<>();
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        System.out.println("How Many Friends : ");
        int n = Integer.parseInt(br.readLine());
        System.out.println("Enter Friends Name : ");
        for (int i = 0; i < n; i++) {
            String friend = br.readLine();
            al.add(friend);
        }
        System.out.println("Collection of friends : ");
        Iterator itr = al.iterator();
        while (itr.hasNext()) {
            System.out.println(itr.next());
        }
    }
}

2. Create the hash table that will maintain the mobile number and student name. Display the contact
list.


import java.util.*;
import java.io.*;

class StudentHashTableNew {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        Hashtable ht = new Hashtable();
        String ch = " ";

        do {
            System.out.print("Enter Student Name : ");
            String Sname = br.readLine();
            System.out.print("Enter Mobile No : ");
            String MobNo = br.readLine();
            ht.put(Sname, MobNo);

            System.out.println("Continue Y/N : ");
            ch = br.readLine();

        } while (ch.equals("Y"));

        System.out.println("Name\tMobile Number");
        Set s = ht.keySet();

        Iterator itr = s.iterator();
        while (itr.hasNext()) {
            Object k = itr.next();
            Object v = ht.get(k);
            System.out.println(k + "\t" + v);
        }
        System.out.println("Enter Name to Be Searched : ");
        String k = br.readLine();

        if (ht.containsKey(k)) {
            System.out.println("Mobile no. of " + k + " is " + ht.get(k));

        } else {
            System.out.println("Name " + k + " Not Found.");
        }
    }
}


3.Write a program to sort HashMap by keys and display the details before sorting andafter sorting.


import java.util.Map;
import java.util.HashMap;
import java.util.TreeMap;
import java.util.Iterator;

public class SortHashMapByKeys {
    public static void main(String args[]) {

        HashMap<Integer, String> hm = new HashMap<Integer, String>();

        hm.put(23, "Yash");
        hm.put(17, "Arun");
        hm.put(15, "Swarit");
        hm.put(9, "Neelesh");

        Iterator<Integer> it = hm.keySet().iterator();
        System.out.println("Before Sorting");

        while (it.hasNext()) {
            int key = (int) it.next();
            System.out.println("Roll no:  " + key + "     name:   " + hm.get(key));
        }
        System.out.println("\n");

        Map<Integer, String> map = new HashMap<Integer, String>();
        System.out.println("After Sorting");

        TreeMap<Integer, String> tm = new TreeMap<Integer, String>(hm);
        Iterator itr = tm.keySet().iterator();
        while (itr.hasNext()) {
            int key = (int) itr.next();
            System.out.println("Roll no:  " + key + "     name:   " + hm.get(key));
        }
    }
}

4. Write a program in which thread sleep for 6 sec in the loop in reverse order from 100 to 1 and
change the name of thread.


class CFG extends Thread {
	String name;

	CFG(String name) {
		this.name = name;
	}

	public void run() {
		try {
			for (int i = 100; i >= 1; i--) {
				Thread.sleep(60);
				System.out.println(i);
			}
		} catch (Exception e) {
			System.out.println(e);
		}
	}

	public static void main(String[] args) {
		CFG obj = new CFG("Red Thread");
		System.out.println("Thread 1 : " + obj.getName());

		obj.start();

		obj.setName("Blue Thread");
		System.out.println("Thread 1 : " + obj.getName());
	}
}

5. Write a java program to accept names of ‘n’ cities, insert same into array list collection and display 
the contents of same array list, also remove all these elements.

import java.util.*;
import java.io.*;

public class Cities {
    public static void main(String[] args) throws IOException {
        ArrayList al = new ArrayList<>();

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        System.out.println("Enter How Many Cities : ");
        int n = Integer.parseInt(br.readLine());
        System.out.println("Enter Cities : ");
        for (int i = 0; i < n; i++) {
            String city = br.readLine();
            al.add(city);
        }

        System.out.println("Collection Of  Cities : ");
        System.out.println(al);
        System.out.println("After Removing all Cities : ");
        al.clear();
        System.out.println(al);
    }
}
 
6. Write a program to create a new tree set, add some colors (string) and print out the tree set


import java.util.*;
class TreeSet1 {
    public static void main(String args[]) {

        TreeSet<String> al = new TreeSet<String>();
        al.add("Red");
        al.add("Green");
        al.add("Blue");
        al.add("Yellow");

        Iterator<String> itr = al.iterator();
        while (itr.hasNext()) {
            System.out.println(itr.next());
        }
    }
}


7. Write a program in which thread sleep for 10 sec in the loop in reverse order from 50 to 1 and 
change the name of thread.

class CFG extends Thread {
	String name;

	CFG(String name) {
		this.name = name;
	}

	public void run() {
		try {
			for (int i = 50; i >= 1; i--) {
				Thread.sleep(10000);
				System.out.println(i);
			}
		} catch (Exception e) {
			System.out.println(e);
		}
	}

	public static void main(String[] args) {
		CFG obj = new CFG("Red Thread");
		System.out.println("Thread 1 : " + obj.getName());

		obj.start();

		obj.setName("Blue Thread");
		System.out.println("Thread 1 : " + obj.getName());
	}
}

8. Accept ‘n’ integers from the user. Store and display integers in sorted order having proper
 collection class. The collection should not accept duplicate elements

import java.util.*;
import java.io.*;

class SortedElements {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        Set s = new TreeSet<>();

        System.out.println("Enter no. of Integers : ");
        int n = Integer.parseInt(br.readLine());

        for (int i = 0; i < n; i++) {
            System.out.println("Enter Number " + (i + 1) + " : ");
            int x = Integer.parseInt(br.readLine());
            s.add(x);
        }

        System.out.println("Elements in set : ");
        Iterator itr = s.iterator();
        while (itr.hasNext()) {
            System.out.println(itr.next());

        }
        System.out.println("Enter element to be search : ");
        int no = Integer.parseInt(br.readLine());

        if (s.contains(no)) {
            System.out.println("Number " + no + " found");

        } else {
            System.out.println("Number " + no + " not found");
        }

    }
}

9. Write a program to display information about the database and list all the tables in the database.
 (Use DatabaseMetaData).
import java.sql.*;

public class a2 {
    public static void main(String[] args) throws Exception {
        ResultSet rs;
        try {
            Class.forName("org.postgresql.Driver");
            System.out.println("Drivers loaded Succesfully");
            Connection c = DriverManager.getConnection("jdbc:postgresql://localhost:5432/demo1", "postgres",
                    "PASSWORD");
            System.out.println("Connection Established succesffull");
            DatabaseMetaData dbmd = c.getMetaData();
            System.out.println("Database Product Name:" + dbmd.getDatabaseProductName());
            System.out.println("User Name:" + dbmd.getUserName());
            System.out.println("Database Driver Name:" + dbmd.getDriverName());
            System.out.println("Database Driver Version:" + dbmd.getDriverVersion());
            System.out.println("Database Product Name:" + dbmd.getDatabaseProductName());
            System.out.println("Database Version:" + dbmd.getDriverMajorVersion());
            rs = dbmd.getTables(null, null, null, new String[] { "TABLE" });
            System.out.println("List of Tables");
            while (rs.next()) {
                String tblName = rs.getString("TABLE_NAME");
                System.out.println("Table:" + tblName);
            }
            c.close();
        } catch (Exception e) {
            System.out.println(e);
        }
    }
}

10. Write a program to display information about all columns in the DONAR table using
 ResultSetMetaData.
import java.sql.*;
import java.io.*;

public class a3 {
  public static void main(String[] args) throws Exception {
    try {
      Statement stmt;
      Class.forName("org.postgresql.Driver");
      Connection conn = DriverManager.getConnection("jdbc:postgresql://localhost:5432/demo1", "postgres", "PASSWORD");
      System.out.println("Connection Established succesffull");
      stmt = conn.createStatement();
      ResultSet rs = stmt.executeQuery("Select * from Project");
      java.sql.ResultSetMetaData rsmd = rs.getMetaData();
      int noOfColumns = rsmd.getColumnCount();
      System.out.println("Number of columns = " + noOfColumns);
      for (int i = 1; i <= noOfColumns; i++) {
        System.out.println("Column No : " + i);
        System.out.println("Column Name : " + rsmd.getColumnName(i));
        System.out.println("Column Type : " + rsmd.getColumnTypeName(i));
        System.out.println("Column display size : " + rsmd.getColumnDisplaySize(i));
      }
      conn.close();
    } catch (Exception e) {
      System.out.println(e);
    }
  }
}

--------------------------------------------------------------------------------------------
1.Write a program to create link list of integer objects. Do the following:
i. add element at first position
ii. delete last element
iii. display the size of link list

public class practical {
    public static void main(String[] args) {
        LinkedList<Integer> al = new LinkedList<Integer>();
        al.add(54);
        al.add(55);
        al.add(56);
        al.add(57);

        System.out.println("Linked List of Integer Object");
        Iterator<Integer> a = al.iterator();
        while (a.hasNext()) {
            System.out.println(a.next());

        }

        System.out.println("Adding element at first position");
        al.addFirst(53);

        Iterator<Integer> b = al.iterator();
        while (b.hasNext()) {
            System.out.println(b.next());

        }

        System.out.println("Deleting element at last position");
        al.removeLast();

        Iterator<Integer> c = al.iterator();
        while (c.hasNext()) {
            System.out.println(c.next());

        }

        System.out.println("Size of Linked List is : " + al.size());
    }
}

2.Create a PROJECT table with fields project_id, Project_name, Project_description,Project_Status. 
etc. Insert values in the table. Display all the details of the PROJECTtable 

import java.sql.*;

class a1 {
    public static void main(String[] args) throws Exception {
        Statement s;
        ResultSet r;
        try {
            Class.forName("org.postgresql.Driver");
            System.out.println("Drivers loaded Succesfully");
            Connection c = DriverManager.getConnection("jdbc:postgresql://localhost:5432/demo1", "postgres",
                    "PASSWORD");
            System.out.println("Connection Established succesffull");
            s = c.createStatement();
            s.executeUpdate(
                    "Create table Project(project_id int primary key,project_name varchar(50),project_desc text,project_status varchar(20))");
            s.executeUpdate("insert into Project values(1,'online Banking System','Java','Incomplete')");
            s.executeUpdate("insert into Project values(2,'Library management System','PHP','Complete')");
            s.executeUpdate("insert into Project values(3,'E form management','Python','Complete')");
            System.out.println("----------------------------");
            r = s.executeQuery("select * from Project");
            while (r.next()) {
                System.out.println("Project ID:" + r.getInt(1));
                System.out.println("Project Name:" + r.getString(2));
                System.out.println("Project Description:" + r.getString(3));
                System.out.println("Project Status:" + r.getString(4));
                System.out.println("----------------------------");
            }
        } catch (Exception e) {
            System.out.println(e);
        }
    }
}


3. Create a MOBILE table with fields Model_Number, Model_Name, Model_Color, Sim_Type, 
NetworkType, BatteryCapacity, InternalStorage, RAM and ProcessorType. Insert values in the 
table. Write a menu driven program to pass the input using Command line argument to perform
the following operations on MOBILE table. 1. Search 2.View All 3. Exit


4. Write a program to calculate the sum and average of an array of 1000 integers (generated
 randomly) using 10 threads. Each thread calculates the sum of 100 integers. Use these values
 to calculate average. [Use join method ].


import java.util.*;

class thread implements Runnable {
    Thread t;
    int i, no, sum;
    int a[] = new int[1000];

    thread(String s, int n) {
        Random rs = new Random();
        t = new Thread(this, s);
        no = n;
        int j = 0;
        for (int i = 1; i <= 1000; i++) {
            a[j] = rs.nextInt() % 100;
            j++;
        }
        t.start();
    }

    public void run() {
        for (int i = 0; i < 100; i++) {
            sum = sum + a[no];
            no++;
        }
        System.out.println("Sum = " + sum);
        System.out.println("Avg = " + (sum / 100));
    }
}

public class calculate_thread {
    public static void main(String[] args) throws InterruptedException {
        thread t1 = new thread("a", 1);
        t1.t.join();

        thread t2 = new thread("b", 100);
        t2.t.join();

        thread t3 = new thread("c", 200);
        t3.t.join();

        thread t4 = new thread("d", 300);
        t4.t.join();

        thread t5 = new thread("e", 400);
        t5.t.join();

        thread t6 = new thread("f", 500);
        t6.t.join();

        thread t7 = new thread("g", 600);
        t7.t.join();

        thread t8 = new thread("h", 700);
        t8.t.join();

        thread t9 = new thread("i", 800);
        t9.t.join();

        thread t10 = new thread("j", 900);
        t10.t.join();

    }

}

5. Create a MOBILE table with fields Model_Number, Model_Name, Model_Color, Sim_Type,
 NetworkType, BatteryCapacity, InternalStorage, RAM and ProcessorType. Insert values in the
 table. Write a menu driven program to pass the input using Command line argument to perform 
 the following operations on MOBILE table. 1. Insert 2.View All 3. Exit

import java.sql.*;
import java.util.*;

class b1 {
	public static void main(String[] args) throws Exception {
		Statement s;
		ResultSet r;
		int mid, ch, did, sid, mno;
		PreparedStatement ps1 = null, ps2 = null;
		try {
			Class.forName("org.postgresql.Driver");
			System.out.println("Drivers loaded Succesfully");
			Connection c = DriverManager.getConnection("jdbc:postgresql://localhost:5432/demo1", "postgres",
					"PASSWORD");
			System.out.println("Connection Established succesffully");
			s = c.createStatement();
			// s.executeUpdate("Create table mobile(mobile_number int primary key,model_name
			// varchar(50),model_color varchar(50),network_type varchar(50),battery_capacity
			// text,internal_storage varchar(20),ram int,processor_type text)");
			Scanner sc = new Scanner(System.in);
			System.out.println("Enter your choice");
			System.out.println("1.Insert");
			System.out.println("2.View");
			System.out.println("3.exit");
			System.out.println("4.modify");
			System.out.println("5.delete");
			System.out.println("6.search");
			ch = sc.nextInt();
			switch (ch) {
				case 1:
					s.executeUpdate(
							"insert into mobile values(4588,'galaxy s10 mini','black','airtel','4G','64GB','8','snapdragon')");
					s.executeUpdate(
							"insert into mobile values(123664,'galaxy s10','blue','airtel','3G','64GB','4','snapdragon888')");
					s.executeUpdate(
							"insert into mobile values(158789,'iphone 13 mini','black','airtel','5G','64GB','8','IOS')");
					break;
				case 2:
					r = s.executeQuery("select * from mobile");
					while (r.next()) {
						System.out.println("model no:" + r.getInt(1));
						System.out.println("model Name:" + r.getString(2));
						System.out.println("model color:" + r.getString(3));

						System.out.println("network type:" + r.getString(4));
						System.out.println("battery capacity:" + r.getString(5));
						System.out.println("Internal Storage:" + r.getString(6));
						System.out.println("RAM:" + r.getString(7));
						System.out.println("Proccesor type:" + r.getString(8));
						System.out.println("----------------------------");
					}
					break;
				case 3:
					System.out.println("good bye ");
					break;
				case 4:
					System.out.println("Enter Mobile Number to be Modified");
					mno = sc.nextInt();

					ps2 = c.prepareStatement("Update Mobile set mname = 'galaxy' where mno=mno");

					System.out.println("Record Modified Successfully ");
					break;
				case 5:
					System.out.println("enter id to be deleted");
					did = sc.nextInt();
					r = s.executeQuery("delete from mobile where mobile_number =" + did);
					break;
				case 6:
					System.out.println("enter id to be search");
					sid = sc.nextInt();
					r = s.executeQuery("select * from mobile where mobile_number =" + sid);
					while (r.next()) {
						System.out.println("model no:" + r.getInt(1));
						System.out.println("model Name:" + r.getString(2));
						System.out.println("model color:" + r.getString(3));

						System.out.println("network type:" + r.getString(4));
						System.out.println("battery capacity:" + r.getString(5));
						System.out.println("Internal Storage:" + r.getString(6));
						System.out.println("RAM:" + r.getString(7));
						System.out.println("Proccesor type:" + r.getString(8));
						System.out.println("----------------------------");
					}
					break;
			}
		} catch (Exception e) {
			System.out.println(e);
		}
	}
}


6. Write a program that implements a multi-thread application that has two threads. First thread 
 generates random integer every 1 second and second thread computes the square of the number
 and prints.

class Square extends Thread {
    int x;

    Square(int n) {
        x = n;
    }

    public void run() {
        int c = x * x;
        System.out.println("Square " + c);
        System.out.println("***************");
    }
}

class Cube extends Thread {
    int x;

    Cube(int n) {
        x = n;
    }

    public void run() {
        int c = x * x * x;
        System.out.println("Cube " + c);
        System.out.println("***************");
    }
}

class Number extends Thread {
    public void run() {

        for (;;) {
            int ri = (int) Math.round(Math.random() * 100);
            System.out.println("Integer is " + ri);
            if (ri % 2 == 0) {
                System.out.println(ri + " Even");
                Square s = new Square(ri);
                s.start();

            } else {
                System.out.println(ri + " Odd");
                Cube c = new Cube(ri);
                c.start();
            }
            try {
                Thread.sleep(1000);
            } catch (InterruptedException e) {

            }
        }

    }
}

public class Test1 {
    public static void main(String[] args) {
        Number n = new Number();
        n.start();
    }

}

7. Write a program for a simple search engine. Accept a string to be searched. Search for the string
 in all text files in the current folder. Use a separate thread for each file. The result should display
 the filename, line number where the string is found.


import java.io.*;

public class SearchThread extends Thread {
    File f1;
    String fname;
    static String str;
    String line;
    LineNumberReader reader = null;

    SearchThread(String fname) {
        this.fname = fname;
        f1 = new File(fname);
    }

    public void run() {
        try {
            FileReader fr = new FileReader(f1);
            reader = new LineNumberReader(fr);
            while ((line = reader.readLine()) != null) {
                if (line.indexOf(str) != -1) {
                    System.out.println("string found in " + fname + " at " + reader.getLineNumber() + " line");
                    break;
                }
            }
        } catch (Exception e) {
        }
    }

    public static void main(String[] args) throws IOException {
        Thread t[] = new Thread[20];
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        System.out.println("Enter String to search");
        str = br.readLine();

        FilenameFilter filter = new FilenameFilter() {
            public boolean accept(File file, String name) {
                if (name.endsWith(".txt")) {
                    return true;
                } else {
                    return false;
                }
            }
        };

        File dir1 = new File(".");
        File[] files = dir1.listFiles(filter);
        if (files.length == 0) {
            System.out.println("no files available with this extension");
        } else {
            for (int i = 0; i < files.length; i++) {
                for (File aFile : files) {
                    t[i] = new SearchThread(aFile.getName());
                    t[i].start();
                }
            }
        }
    }
}


8. Using the concepts of thread synchronization create two threads as sender and receiver. Sender 
 thread will set a message to the receiver thread that will display the message on console. The 
 sender thread accepts the input message from console. Continue this process until sender sets
 the message as “Good Bye Corona”. 


import java.util.*;

class SenderThread extends Thread {
    public void run() {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter Message : ");
        String msg = sc.nextLine();

        RecieverThread rt = new RecieverThread(msg);
        rt.start();
    }

}

class RecieverThread extends Thread {
    String msg;

    RecieverThread(String msg) {
        this.msg = msg;

    }

    public void run() {
        if (msg.compareTo("GoodByeCorona") != 0) {
            System.out.println("The Message is " + msg);
        }
    }

}

public class ThreadSyc {
    public static void main(String[] args) {
        SenderThread st = new SenderThread();
        st.start();

    }
}

9. Create a java application to store city names and their STD codes using an appropriate Collection.
 The GUI should allow the following operations: i. Add a new city and its code (No duplicates)
 ii. Remove a city from the collection iii. Search for a city name and display the code


10. Program to define a thread for printing text on output screen for ‘n’ number of times. Create 3 
 threads and run them. Pass the text ‘n’ parameters to the thread constructor. Example: i. First 
 thread prints “COVID19” 10 times. ii. Second thread prints “LOCKDOWN2020” 20 times iii. Third 
 thread prints “VACCINATED2021” 30 times
import java.io.*;
import java.lang.String.*;

class FirstThread extends Thread {
    int n;

    FirstThread(int n) {
        this.n = n;
    }

    public void run() {
        try {
            for (int i = 1; i <= n; i++) {
                System.out.println("COVID19 = " + i);
                Thread.sleep(1000);
            }

        } catch (Exception e) {
            System.out.println(e);
        }

    }

}

class SecondThread extends Thread {
    int n;

    SecondThread(int n) {
        this.n = n;
    }

    public void run() {
        try {
            for (int i = 1; i <= n; i++) {
                System.out.println("LOCKDOWN2020 = " + i);
                Thread.sleep(1000);
            }

        } catch (Exception e) {
            System.out.println(e);
        }

    }

}

class ThirdThread extends Thread {
    int n;

    ThirdThread(int n) {
        this.n = n;
    }

    public void run() {
        try {
            for (int i = 1; i <= n; i++) {
                System.out.println("VACCINATED2021 = " + i);
                Thread.sleep(1000);
            }

        } catch (Exception e) {
            System.out.println(e);
        }

    }

}

public class Covid {
    public static void main(String[] args) {
        FirstThread t1 = new FirstThread(10);
        t1.start();

        SecondThread t2 = new SecondThread(20);
        t2.start();

        ThirdThread t3 = new ThirdThread(30);
        t3.start();
    }

}